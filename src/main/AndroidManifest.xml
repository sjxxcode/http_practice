<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sj.http_practice">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".okhttp.get.GetAct"/>
        <activity android:name=".okhttp.post.PostAct"/>
        <activity android:name=".okhttp.head.Head"/>
        <activity android:name=".okhttp.header.HeaderAct"/>
        <activity android:name=".okhttp.cache.CacheAct"/>
        <activity android:name=".okhttp.cancel.CancelAct"/>
        <activity android:name=".okhttp.timeout.TimeOutAct"/>
        <activity android:name=".okhttp.authentication.AuthenticationAct"/>
    </application>

</manifest>